import { environment as env } from '../../environments/environment';

export const API_URL = `${env.apiUrl}`;
export const PRODUCTS_URL = `${API_URL}/products`;
export const STORES_URL = `${API_URL}/stores`;
